// В данном примере JavaScript не обязателен, но если нужно добавить интерактивность, можно использовать его.
// Например, для управления воспроизведением видео или анимациями.

document.addEventListener('DOMContentLoaded', function() {
    const video = document.getElementById('bg-video');
    video.play();
});